<!DOCTYPE html>
<html>

<head>
	<meta charset="utf-8" />
	<title>My Discord</title>
</head>

<body>
	<h1>Channel list :</h1>
	<% if (list.length == 0) { %>
		<a> No channel ! </a>
		<% } else { %>
			<ul>
				<% for (var i = 0; i < list.length; i++) { %>
					<li>
						<a href="/<%= list[i] %>">
							<%= list[i] %>
						</a>
					</li>
					<% }; %>
			</ul>
			<% } %>
				<br>
				<br>
				<form action="/" method="post">
					<input type="text" name="new_channel" placeholder="New channel" size="25" autofocus />
					<input type="submit" value="Create" />
				</form>
</body>

</html>
